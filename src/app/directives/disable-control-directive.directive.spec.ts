import { DisableControlDirectiveDirective } from './disable-control-directive.directive';

describe('DisablControlDirectiveDirective', () => {
    it('should create an instance', () => {
        const directive = new DisableControlDirectiveDirective(null);
        expect(directive).toBeTruthy();
    });
});